LAST_COMMIT_MSG=$(git log -1 --pretty=%B)

if echo "$LAST_COMMIT_MSG" | grep -q "Atualiza changelog"; then
    exit 0
fi

node .husky/commit-form.js

git add CHANGELOG.md

git commit -m "Atualiza changelog"

git push
