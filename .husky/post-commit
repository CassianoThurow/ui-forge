# Verificar se o commit contém mudanças no CHANGELOG.md
if git diff --cached --name-only | grep -q "CHANGELOG.md"; then
    # Emendar o commit para incluir o changelog gerado
    git commit --amend --no-edit --allow-empty

    # Push das mudanças
    git push --force-with-lease
fi